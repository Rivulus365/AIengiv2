// Firebase integration has been disabled.
// This file is kept as a placeholder to prevent import errors in legacy code,
// though all active imports should now be pointing to services/auth.ts or removed.

export const auth = null;
export default null;